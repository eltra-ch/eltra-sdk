<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:EltraNavigo.Views.Converters"
             x:Class="EltraNavigo.Views.Pvm.Status.PvmStatusView">
  <ContentView.Resources>
      <converters:StatusWordToImageConverter x:Key="StatusWordToImageConverter"/>
    </ContentView.Resources>
    <ContentView.Content>      
      <Grid>
          <Grid.RowDefinitions>
              <RowDefinition Height="Auto"></RowDefinition>
              <RowDefinition Height="Auto"></RowDefinition>
              <RowDefinition Height="Auto"></RowDefinition>              
          </Grid.RowDefinitions>
          
          <Grid.ColumnDefinitions>
              <ColumnDefinition Width="4"></ColumnDefinition>
              <ColumnDefinition Width="Auto"></ColumnDefinition>
              <ColumnDefinition></ColumnDefinition>
          </Grid.ColumnDefinitions>
                    
          <!-- Target reached -->
          <Image Grid.Column="1" Grid.Row="0" HeightRequest="32" Source="{Binding StatusWord, Converter={StaticResource StatusWordToImageConverter}, ConverterParameter=10}"></Image>
          <Label Grid.Column="2" Grid.Row="0" Text="Target reached" VerticalOptions="Center"/>
            <!-- Speed is equal 0 -->
          <Image Grid.Column="1" Grid.Row="1" HeightRequest="32" Source="{Binding StatusWord, Converter={StaticResource StatusWordToImageConverter}, ConverterParameter=12}"></Image>
          <Label Grid.Column="2" Grid.Row="1" Text="Speed is equal 0" VerticalOptions="Center"/>
            <!-- Speed is limited -->
          <Image Grid.Column="1" Grid.Row="2" HeightRequest="32" Source="{Binding StatusWord, Converter={StaticResource StatusWordToImageConverter}, ConverterParameter=11}"></Image>
          <Label Grid.Column="2" Grid.Row="2" Text="Speed is limited" VerticalOptions="Center"/>
        </Grid>
  </ContentView.Content>
</ContentView>