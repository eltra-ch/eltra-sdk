<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:helpers="clr-namespace:EltraNotKauf.Controls.Helpers;assembly=EltraNotKauf"
             x:Class="EltraNotKauf.Views.ConnectionStatus.ConnectionStatusView">
  <ContentView.Content>
        <StackLayout Orientation="Vertical">
            <Frame Padding="4" BackgroundColor="Transparent" />
            <Image HeightRequest="32" WidthRequest="32" Aspect="AspectFit" HorizontalOptions="EndAndExpand">
                <Image.Triggers>
                    <DataTrigger TargetType="Image" Binding="{Binding IsConnected}" Value="true">
                        <Setter Property="IsVisible" Value="False"/>
                    </DataTrigger>
                    <DataTrigger TargetType="Image" Binding="{Binding IsConnected}" Value="false">
                        <Setter Property="Source" Value="{helpers:ImageResource EltraNotKauf.Resources.cloud-storm.png}"/>
                    </DataTrigger>
                </Image.Triggers>
            </Image>
        </StackLayout>
    </ContentView.Content>
</ContentView>